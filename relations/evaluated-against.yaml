id: core.rel.evaluated-against
symbol: evaluatedAgainst
version: 0.1.0
status: experimental

definition: Identifies the DesiredState or Direction used as the comparison target.

source:
  - Progress
  - StateChange
  - Trajectory

target:
  - DesiredState
  - Direction

direction: directed
provenance: optional
authority_effect: none
