id: core.rel.specifies
symbol: specifies
version: 0.1.0
status: experimental

definition: Associates a Goal with the DesiredState that makes its direction inspectable.

source:
  - Goal

target:
  - DesiredState

direction: directed
provenance: optional
authority_effect: none

inverse: desiredBy
