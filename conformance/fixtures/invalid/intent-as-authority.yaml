id: core.fixture.invalid.intent-as-authority
version: 0.1.0
status: illustrative
description: The fact that an Actor intends an Action is used as authorization for that Action.
expected: invalid
objects:
- id: intent:delete-record
  type: core.intent
  attributes:
    represented_as: core.authority
- id: action:delete-record
  type: core.action
relations:
- relation: core.rel.authorized-by
  source: action:delete-record
  target: intent:delete-record
expectations:
  rules_pass: []
  rules_fail:
  - core.rule.capability-authority-separation
  boundaries_triggered:
  - core.boundary.intent-not-authority
